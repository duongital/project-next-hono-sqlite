# Better Auth Configuration
# Generate a random secret: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here
BETTER_AUTH_URL=http://localhost:8787

# Email Configuration (Cloudflare Mailchannels)
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=Your App Name

# Optional: DKIM Configuration for production email signing
# Generate DKIM keys: openssl genrsa -out private.key 2048
# Then extract the private key base64
# MAILCHANNELS_DKIM_DOMAIN=yourdomain.com
# MAILCHANNELS_DKIM_SELECTOR=mailchannels
# MAILCHANNELS_DKIM_PRIVATE_KEY=your-dkim-private-key
